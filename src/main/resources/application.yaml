spring:
  application:
    name: analytics
  datasource:
    url: jdbc:postgresql://localhost:5433/spotlight
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver

server:
  port: 8090
